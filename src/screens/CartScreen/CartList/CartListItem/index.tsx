import { Text, TouchableOpacity } from 'react-native';
import React from 'react';

const CartListItem = () => {
  return (
    <TouchableOpacity onPress={() => {}}>
      <Text>CartListItem</Text>
    </TouchableOpacity>
  );
};

// const styles = StyleSheet.create({});

export default CartListItem;
